# myemacs
my emacs setup and configuration for daily development

Usage:
I use emacs that has compiled from source code by myself. 

git clone https://github.com/ice-zhao/myemacs.git

shopt -s dotglob

mv myemacs/* ~/installed/emacs/

shopt -u dotglob

Note: Ensure all hidden files exist.
